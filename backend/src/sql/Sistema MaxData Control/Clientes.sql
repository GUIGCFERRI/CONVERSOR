SELECT
    COD_CLI AS CODIGO,
    NOME_CLI AS NOME,
    NOME_FANTASIA AS FANTASIA,
    CONTATO_CLI AS CONTATO,
    CNPJ_CLI AS CNPJ_CNPF,
    case when DOC_CLI = '' or DOC_CLI is null then INSC_ESTADUAL else DOC_CLI END AS IE_RG,
    ENDRES_CLI AS ENDERECO,
    PONTO_REFERENCIA AS COMPLEMENTO,
    BAIRES_CLI AS BAIRRO,
    NUMRES_CLI AS NUMERO,
    CIDRES_CLI AS CIDADE,
    ESTRES_CLI AS UF,
    CEPRES_CLI AS CEP,
    TELRES_CLI AS TELEFONE,
    CELULAR_CLI AS CELULAR,
    EMAIL_CLI AS EMAIL,
    PAI_CLI AS PAI,
    MAE_CLI AS MAE,
    CONJUGE_CLI AS CONJUGE,
    ESTCIVIL_CLI AS EST_CIVIL,
    LIMITE_CLI AS LIMITE_CREDITO,
    OBS_CLI AS OBSERVACOES,
    NASCIMENTO_CLI AS NASCIMENTO,
    DATACADASTRO_CLI AS CADASTRO,
    case when ATIVO_CLI = 'S' then 'Ativo' else 'Inativo' end as SITUACAO
FROM CLIENTE