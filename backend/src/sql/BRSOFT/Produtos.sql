SELECT
  `idProduto` AS CODIGO,
  `descricao` AS DESCRICAO,
  `ncm` AS COD_NCM,
  `cst` AS ST,
  `custoMedio` AS CUSTO_MEDIO,
  `precoCompra` AS PRECO_CUSTO,
  `estoqueAtual` AS QTD,
  `valorComAvista` AS PRECO_VENDA,
  `cest` AS PERSONAL6
FROM PRODUTO
