SELECT
  `codigo` AS CODIGO,
  `fornecedor` AS FANTASIA,
  `endereco` AS ENDERECO,
  `bairro` AS BAIRRO,
  `cidade` AS CIDADE,
  `estado` AS UF,
  `cep` AS CEP,
  `telefone` AS TELEFONE,
  `fax` AS FAX,
  `cnpj` AS CNPJ_CNPF,
  `ie` AS IE_RG,
  `email` AS EMAIL,
  `obs` AS OBSERVACOES,
  `razao` AS NOME,
  `im` AS IM
  FROM FORNECEDOR
