SELECT
    --F510CLI.CODCLI AS CODIGO,
    F510CLI.RAZCLI AS NOME,
    F510CLI.FANCLI AS FANTASIA,
    F510CLI.ENDEND AS ENDERECO,
    F510CLI.NUMEND AS NUMERO,
    F510CLI.BAIEND AS BAIRRO,
    F510CLI.CEPEND AS CEP,
    F510CLI.FONEMB AS TELEFONE,
    F510CLI.CNTEMB AS CONTATO,
    F510CLI.EMLEMB AS EMAIL,
    F510CLI.CPFCGC AS CNPJ_CNPF,
    F510CLI.NUMIES AS IE_RG,
    F510CLI.OBSCLI AS OBSERVACOES,
    F501CID.NOMCID AS CIDADE,
    F501CID.ESTCID AS UF
FROM F510CLI
LEFT JOIN F501CID ON F510CLI.CODCID = F501CID.CODCID
