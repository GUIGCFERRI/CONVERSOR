SELECT CPFORNECED.CODFOR AS CODIGO,
CPFORNECED.RAZFOR AS NOME,
CPFORNECED.NOMEFOR AS FANTASIA,
CPFORNECED.CNPJFOR AS CNPJ_CNPF,
CPFORNECED.INSCFOR AS IE_RG,
CPFORNECED.ENDFOR AS ENDERECO,
CPFORNECED.NUMFOR AS NUMERO,
CPFORNECED.COMPLFOR AS COMPLEMENTO,
CPFORNECED.BAIRFOR AS BAIRRO,
CPFORNECED.CEPFOR AS CEP,
CPFORNECED.CIDFOR AS CIDADE,
CPFORNECED.UFFOR AS UF,
CPFORNECED.CONTFOR AS CONTATO,
CPFORNECED.FONEFOR AS TELEFONE,
CPFORNECED.FAXFOR AS FAX,
CPFORNECED.EMAILFOR AS EMAIL
FROM CPFORNECED
